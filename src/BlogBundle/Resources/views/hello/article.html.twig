{#
	src/BlogBundle/Resources/views/Hello/article.html.twig
	Etend src/BlogBundle/Resources/views/layout.html.twig
	webDev - 2017
	Variables expos�es :
		$id => Identifiant d�fini par la route, et transmis par voirAction()
#}
	{% extends "BlogBundle::layout.html.twig" %}
	
	{% block title %}{{ parent() }} - Article : {{ article.getId() }} {% endblock %}
	
	{% block blog_body %}
	<main>
		<article>
			<header>
				<h1>
				 	  {{ article.getTitre() }}
				</h1>
			</header>
			<p>
		    {{ article.getContenu() }}
			</p>
	
	
		
			<footer>
				<cite>Auteur :{{ article.getAuteur }}</cite>
				Date: {{ (article.getDate() | date ("d-m-Y H:i")) }}
				<p>Vu <span class="badge">{{ article.getVues() }}</span>fois.</p>
			</footer>
		</article>
	</main>
	{% endblock %}